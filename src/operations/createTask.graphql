mutation createTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    task {
      rowId
      number
      content
      description
      priority
      columnId
      projectId
      createdAt
    }
  }
}
